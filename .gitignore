# Cache files (large binary files for ZK proofs)
# These are generated during build and NOT committed to git
# They are served from public/cache/ via API route during runtime
ui/public/cache/*
# Keep directory structure and documentation
!ui/public/cache/.gitkeep
!ui/public/cache/README.md
.vercel
server/public/cache/
server/node_modules/
logs/
node_modules/
