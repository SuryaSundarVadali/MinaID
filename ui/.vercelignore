# Don't ignore cache files - we need them deployed!
# They're served via /api/cache/ route from public/cache/

# Node modules are handled automatically
node_modules/

# Test files
__tests__/
*.test.ts
*.test.tsx
